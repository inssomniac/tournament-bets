# UI Audit Report — Летний Кубок по лапте 2026

## Методология
Аудит проведён по исходному коду всех пользовательских страниц.  
Критерии: шрифты (Russo One / Oswald vs системный), отступы, цвета (var vs hardcoded), консистентность стилей.

---

## Найденные проблемы

### 🔴 P0 — Видно сразу, портит впечатление

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `Balance.tsx` | 59 | `"очков"` — `<p>` без шрифта, рендерится в системном шрифте рядом с Russo One балансом |
| 2 | `Balance.tsx` | 72 | `"Ставок ещё нет"` — `<p>` без шрифта, в системном шрифте |
| 3 | `Home.tsx` | 59 | `"очков"` — `<p>` без шрифта рядом с Russo One балансом (та же проблема что в Balance) |
| 4 | `Registration.tsx` | 52 | `marginTop: -26` — старое значение, контент может обрезаться под диагональным хедером. Все остальные страницы уже переведены на `-10` |

### 🟡 P1 — Шрифтовые нарушения в карточках и интерфейсе

| # | Файл | Строки | Проблема |
|---|------|--------|----------|
| 5 | `Balance.tsx` | 89–90 | Название матча в карточке ставки (`fontWeight: 600`, без класса) — системный шрифт |
| 6 | `Balance.tsx` | 96–97 | `"Ставка на: teamName"` — системный шрифт |
| 7 | `Balance.tsx` | 106–108 | Сумма pending ставки — `<p>` без шрифта |
| 8 | `Home.tsx` | 75 | Подписи статов (`"Выиграно"` / `"В игре"` / `"Проиграно"`) — `<p>` без шрифта |
| 9 | `Matches.tsx` | 235–238 | Строка суммы ставки в карточке (`{bet.amount} оч → {bet.potential_win} потенциально`) — системный шрифт |
| 10 | `Leaderboard.tsx` | 137–138 | Плавающий бар `"Вы 👈"` — `fontWeight: 600` без класса шрифта |
| 11 | `BetFlow.tsx` | 108–111 | Информация о текущей ставке в dodep блоке — системный шрифт |
| 12 | `Registration.tsx` | 54 | `"Твой стартовый банк"` — системный шрифт |

### 🟢 P2 — Мелкие несоответствия

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 13 | `Matches.tsx` | 109 | `"Матчей нет"` — `<p>` без шрифта (аналогично "Ставок ещё нет") |
| 14 | `BetFlow.tsx` | 151 | Дисклеймер `"⚡ Коэф динамический..."` — системный шрифт на синем фоне |
| 15 | `Registration.tsx` | 59 | Описание "Делай ставки..." — системный шрифт (body text, допустимо, но непоследовательно) |

---

## Итого
- **4 критичных** проблемы (P0) — видны без взаимодействия
- **8 проблем** (P1) — видны при использовании
- **3 мелких** (P2)

Все проблемы — отсутствие `className="oswald"` или `fontFamily` на элементах, или старый отступ в Registration.
